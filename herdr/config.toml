# herdr config — overrides only; anything omitted uses herdr's defaults.

[keys]
# Leader key. ctrl+a is far easier to reach than the default ctrl+b.
# Press ctrl+a, release, then the command key.
prefix = "ctrl+a"

# Move between spaces (the left-nav entries) with the arrow keys.
# Hammerspoon already maps ctrl+i -> Up and ctrl+k -> Down globally, so in
# practice this is: ctrl+a, then ctrl+i (up the list) / ctrl+k (down the list).
previous_workspace = "prefix+up"
next_workspace     = "prefix+down"

# Move between tabs with left/right arrows — completes the compass:
# up/down = spaces, left/right = tabs. Hammerspoon maps ctrl+j -> Left and
# ctrl+l -> Right, so in practice: ctrl+a, then ctrl+j (prev tab) / ctrl+l (next tab).
previous_tab = "prefix+left"
next_tab     = "prefix+right"

# ctrl+w closes the *tab* (the short-lived thing), matching browser/editor muscle
# memory. Closing a whole workspace stays on herdr's default prefix+shift+d —
# deliberate and rarely hit, so a reflexive ctrl+w can't nuke a long-lived space.
close_tab = "prefix+ctrl+w"

[session]
# After a herdr server (or Mac) restart, resume supported agent panes back into
# their native conversation sessions instead of dead shell prompts. Relies on
# official integrations that report session refs (claude does).
resume_agents_on_restore = true
